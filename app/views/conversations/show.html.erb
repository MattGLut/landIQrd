<% content_for :title, @conversation.title %>

<div class="lg:hidden">
  <%= link_to "← Back to messages", conversations_path, class: "mb-4 inline-block text-sm text-brand-600 dark:text-brand-400 hover:text-brand-700 dark:hover:text-brand-300" %>
</div>

<%= render "conversations/thread", conversation: @conversation, messages: @messages, message: @message %>
