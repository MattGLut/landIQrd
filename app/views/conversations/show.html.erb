<% content_for :title, @conversation.title %>

<div class="lg:hidden">
  <%= link_to "← Back to messages", conversations_path, class: "mb-4 inline-block text-sm text-accent hover:text-accent-hover" %>
</div>

<%= render "conversations/thread", conversation: @conversation, messages: @messages, message: @message %>
