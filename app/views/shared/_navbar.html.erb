<nav class="border-b border-gray-200 bg-white">
  <div class="mx-auto flex max-w-6xl items-center justify-between px-4 py-3">
    <div class="flex items-center gap-6">
      <%= link_to "Prop Man", authenticated_root_path, class: "text-lg font-semibold text-indigo-600" %>
      <div class="hidden gap-4 sm:flex">
        <%= nav_link "Dashboard", authenticated_root_path %>
        <% if current_user.landlord? %>
          <%= nav_link "Properties", properties_path %>
        <% end %>
        <%= nav_link "Work Orders", work_orders_path %>
        <%= nav_link "Messages", conversations_path %>
        <% if current_user.admin? %>
          <%= nav_link "Admin", admin_dashboard_path %>
        <% end %>
      </div>
    </div>

    <div class="flex items-center gap-4 text-sm">
      <span class="hidden text-gray-500 sm:inline">
        <%= current_user.display_name %>
        <span class="ml-1 rounded-full bg-gray-100 px-2 py-0.5 text-xs font-medium text-gray-600"><%= current_user.role.titleize %></span>
      </span>
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "text-gray-500 hover:text-gray-900" %>
    </div>
  </div>
</nav>
