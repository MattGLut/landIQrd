<div class="rounded-lg border border-dashed border-border-default bg-surface p-10 text-center">
  <p class="text-muted"><%= message %></p>
  <% if local_assigns[:action].present? %>
    <div class="mt-4"><%= action %></div>
  <% end %>
</div>
