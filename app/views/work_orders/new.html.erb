<% content_for :title, "New work request" %>

<div class="mx-auto max-w-2xl">
  <%= render "shared/page_header", title: "New work request" %>
  <%= render "form", work_order: @work_order, units: @units, form_url: work_orders_path, cancel_url: work_orders_path %>
</div>
